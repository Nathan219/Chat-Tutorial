{
  "name": "chat",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "moment": "^2.21.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "socket.io-client": "^2.0.4"
  },
  "devDependencies": {
    "react-scripts": "1.0.15"
  },
  "scripts": {
    "start": "REACT_APP_SERVER_URL=$SERVER_URL react-scripts start",
    "build": "REACT_APP_SERVER_URL=$SERVER_URL react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "start:production": "serve -s build -p 3000"
  }
}
